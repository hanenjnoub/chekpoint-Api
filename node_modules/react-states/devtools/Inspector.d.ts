import * as React from 'react';
export declare type RenderPaths = {
    [path: string]: (children: React.ReactNode) => React.ReactNode;
};
declare type InspectorProps = {
    value: object;
    expandedPaths: string[];
    delimiter: string;
    small?: boolean;
    onToggleExpand: (path: string[]) => void;
    onClickPath?: (path: string[]) => void;
    renderPaths?: RenderPaths;
    selectedStatePath?: string;
    onSubmitState?: (newState: string) => void;
};
declare const Inspector: ({ value, expandedPaths, small, onToggleExpand, delimiter, onClickPath, renderPaths, selectedStatePath, onSubmitState, }: InspectorProps) => JSX.Element;
export default Inspector;
