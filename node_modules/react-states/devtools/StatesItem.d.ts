import * as React from "react";
import { HistoryItem } from "./Manager";
export declare const StatesItem: React.MemoExoticComponent<({ id, history, transitions, toggleExpanded, isMounted, isExpanded, triggerTransitions, }: {
    id: string;
    toggleExpanded: (id: string) => void;
    history: HistoryItem[];
    transitions: {
        [key: string]: {
            [key: string]: Function;
        };
    };
    isMounted: boolean;
    isExpanded: boolean;
    triggerTransitions: () => void;
}) => JSX.Element>;
