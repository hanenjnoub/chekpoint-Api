import * as React from 'react';
import { HistoryItem } from './Manager';
export declare const ExpandedStates: React.MemoExoticComponent<({ transitions, currentState, history, triggerTransitions, }: {
    transitions: {
        [key: string]: {
            [key: string]: Function;
        };
    };
    currentState: string | symbol;
    history: HistoryItem[];
    triggerTransitions: () => void;
}) => JSX.Element>;
