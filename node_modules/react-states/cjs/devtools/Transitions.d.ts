import * as React from 'react';
export declare const Transitions: React.MemoExoticComponent<({ transitions, currentState, }: {
    transitions: {
        [key: string]: {
            [key: string]: Function;
        };
    };
    currentState: string | symbol;
}) => JSX.Element>;
