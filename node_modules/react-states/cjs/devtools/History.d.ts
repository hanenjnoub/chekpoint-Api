import * as React from 'react';
import { HistoryItem } from './Manager';
export declare const History: React.MemoExoticComponent<({ history, filterIgnored }: {
    history: HistoryItem[];
    filterIgnored: boolean;
}) => JSX.Element>;
